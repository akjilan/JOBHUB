import { getAuth } from 'firebase/auth'
import React from 'react'
import app1 from '../firebase/firebase.config';
const SignupMenual = () => {
    const auth = getAuth(app1);

  return (
    <>
        
    </>
  )
}

export default SignupMenual